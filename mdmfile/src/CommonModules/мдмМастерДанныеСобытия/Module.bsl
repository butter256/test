
Процедура МастерДанныеПередЗаписьюОбработчик(Источник) Экспорт
	
	Если Источник.ЭтоНовый() тогда
		Источник.мдмДатаСоздания = ТекущаяДата();
		Источник.мдмАвторСоздания = ПараметрыСеанса.ТекущийПользователь;
	Иначе 
		Источник.мдмДатаИзменения = ТекущаяДата();
		Источник.мдмАвторИзменения = ПараметрыСеанса.ТекущийПользователь;
	КонецЕсли;	
	
КонецПроцедуры
